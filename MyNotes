python -m venv venv
source venv/bin/activate
python --version
python -m pip install numpy
python -m pip install matplotlib
pip list
python -m pip install --upgrade pip
pip list
python -m pip install numpy==1.26.4
pip list
python -m pip install pycodestyle
python -m pip install pylint
python -m pip freeze > requirements.txt
cat requirements.txt


python -m pip install -r requirements.txt


pylint inflammation

Reports shoddy code 


python -m pip install black


for i in inflammation/*py
do
  black $i
done


git switch -C test-suite

python -m pip install pytest-cov


Test code using the tests:
python -m pytest --cov=inflammation.models tests/test_models.py


Report where code is not covered by tests:
python -m pytest --cov=inflammation.models --cov-report term-missing tests/test_models.py
